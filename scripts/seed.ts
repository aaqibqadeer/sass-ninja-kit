// scripts/seed.ts — CORE (CLAUDE.md §2). Seeds a dev/prod-shaped database with
// baseline data. Per the "new table = three things" rule (§1.4), every new
// model adds its seed entry here in the same commit as its Zod schema and
// adapter method.
//
// Deferred to Phase 2: real seeding once the DB adapter and org schema exist.
export {};
